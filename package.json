{
  "name": "luckmd",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5"
  }
}